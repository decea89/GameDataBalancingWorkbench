<Window x:Class="BalanceForge.Desktop.Views.CloseConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BalanceForge.Desktop.ViewModels"
        Title="Unsaved Changes" Height="200" Width="400" 
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize">
    <Window.DataContext>
        <local:CloseConfirmationDialogViewModel />
    </Window.DataContext>

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="20">
        <TextBlock Text="You have unsaved changes." FontSize="14" FontWeight="Bold" Margin="0,0,0,10" />
        <TextBlock Text="Would you like to save before closing?" TextWrapping="Wrap" Margin="0,0,0,20" Foreground="Gray" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Command="{Binding SaveCommand}" Padding="15,8" Margin="0,0,10,0" IsDefault="True">
                Save
            </Button>
            <Button Command="{Binding DiscardCommand}" Padding="15,8" Margin="0,0,10,0">
                Discard
            </Button>
            <Button Command="{Binding CancelCommand}" Padding="15,8" IsCancel="True">
                Cancel
            </Button>
        </StackPanel>
    </StackPanel>
</Window>
